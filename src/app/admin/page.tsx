"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect, useState } from "react";
type Project = { id?: string; title: string; description: string; tags: string[]; repo_url: string; demo_url: string; markdown_content: string; is_visible: boolean };
const empty: Project = { title: "", description: "", tags: [], repo_url: "", demo_url: "", markdown_content: "", is_visible: true };
export default function Admin() { const [password, setPassword] = useState(""); const [authed, setAuthed] = useState(false); const [projects, setProjects] = useState<Project[]>([]); const [form, setForm] = useState<Project>(empty); async function load() { const r = await fetch("/api/admin/projects"); if (r.ok) { setAuthed(true); setProjects(await r.json()); } } useEffect(() => { load(); }, []); async function login(e: React.FormEvent) { e.preventDefault(); const r = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); if (r.ok) load(); } async function save(e: React.FormEvent) { e.preventDefault(); const r = await fetch("/api/admin/projects", { method: form.id ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); if (r.ok) { setForm(empty); load(); } } async function remove(id: string) { await fetch("/api/admin/projects", { method: "DELETE", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id }) }); load(); } if (!authed) return <main className="admin-page"><h1>Admin sign in</h1><form onSubmit={login} className="admin-form"><input type="password" placeholder="Admin password" value={password} onChange={(e) => setPassword(e.target.value)} /><button>Sign in</button></form></main>; return <main className="admin-page"><h1>Project manager</h1><form onSubmit={save} className="admin-form"><input placeholder="Title" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} required /><input placeholder="Description" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /><input placeholder="Tags, comma separated" value={form.tags.join(", ")} onChange={(e) => setForm({ ...form, tags: e.target.value.split(",").map((tag) => tag.trim()).filter(Boolean) })} /><input placeholder="Repository URL" value={form.repo_url} onChange={(e) => setForm({ ...form, repo_url: e.target.value })} /><input placeholder="Demo URL" value={form.demo_url} onChange={(e) => setForm({ ...form, demo_url: e.target.value })} /><textarea placeholder="Markdown content" value={form.markdown_content} onChange={(e) => setForm({ ...form, markdown_content: e.target.value })} /><label><input type="checkbox" checked={form.is_visible} onChange={(e) => setForm({ ...form, is_visible: e.target.checked })} /> Visible</label><button>{form.id ? "Update project" : "Add project"}</button></form><div className="admin-table">{projects.map((project) => <div className="admin-row" key={project.id}><strong>{project.title}</strong><span>{project.is_visible ? "Visible" : "Hidden"}</span><button onClick={() => setForm(project)}>Edit</button><button onClick={() => project.id && remove(project.id)}>Delete</button></div>)}</div></main>; }
