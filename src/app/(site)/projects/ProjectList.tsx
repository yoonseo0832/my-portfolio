"use client";
import { ExternalLink, GitFork, GitBranch, Star } from "lucide-react";
import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";
import { ProjectItem } from "@/types/project";
const platforms = [{ id: "github", label: "GitHub" }, { id: "notion", label: "Notion" }, { id: "instagram", label: "Instagram" }, { id: "youtube", label: "YouTube" }, { id: "canva", label: "Canva" }];
export default function ProjectList({ projects }: { projects: ProjectItem[] }) { const [filter, setFilter] = useState("all"); const visible = projects.filter((p) => filter === "all" || (p.platform ?? "github") === filter); return <div className="projects-list"><div className="platform-filters"><button className={filter === "all" ? "active" : ""} onClick={() => setFilter("all")}>All <span>{projects.length}</span></button>{platforms.map((platform) => <button key={platform.id} className={filter === platform.id ? "active" : ""} onClick={() => setFilter(platform.id)}>{platform.label} <span>{projects.filter((p) => (p.platform ?? "github") === platform.id).length}</span></button>)}{projects.some((p) => !platforms.some((platform) => platform.id === p.platform)) && <button className={filter === "custom" ? "active" : ""} onClick={() => setFilter("custom")}>Other</button>}</div><div className="projects-grid">{visible.map((p) => <Card key={p.id} className="project-card"><CardHeader><div className="project-card-top"><Badge variant="outline" className="source-badge">{p.platformLabel ?? "GitHub"}</Badge><CardDescription>{new Intl.DateTimeFormat("ko-KR").format(new Date(p.updatedAt))}</CardDescription></div><CardTitle>{p.title}</CardTitle></CardHeader><CardContent><p className="project-description">{p.description}</p><div className="project-tags">{p.tags.map((tag) => <Badge key={tag} variant="secondary">{tag}</Badge>)}</div></CardContent><CardFooter className="project-footer"><span className="project-stats"><span><Star size={14} /> {p.stars}</span><span><GitFork size={14} /> {p.forks}</span></span><span className="project-actions"><a className="project-icon-button" href={p.repoUrl} target="_blank" rel="noopener noreferrer"><GitBranch /></a>{p.demoUrl && <a className="project-icon-button" href={p.demoUrl} target={p.demoUrl.startsWith("/") ? "_self" : "_blank"} rel="noopener noreferrer"><ExternalLink /></a>}</span></CardFooter></Card>)}</div>{visible.length === 0 && <p className="projects-empty">해당 플랫폼의 프로젝트가 없습니다.</p>}</div>; }
