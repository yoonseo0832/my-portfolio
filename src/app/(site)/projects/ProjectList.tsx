"use client";
import { ExternalLink, GitFork, GitBranch, Star } from "lucide-react";
import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { ProjectItem } from "@/types/project";
export default function ProjectList({ projects }: { projects: ProjectItem[] }) { const [filter, setFilter] = useState("all"); const filtered = projects.filter((p) => filter === "all" || p.source === filter); return <div className="projects-list"><Tabs value={filter} onValueChange={setFilter}><TabsList className="projects-tabs"><TabsTrigger value="all">전체 <span>{projects.length}</span></TabsTrigger><TabsTrigger value="github">GitHub <span>{projects.filter((p) => p.source === "github").length}</span></TabsTrigger><TabsTrigger value="custom">커스텀 <span>{projects.filter((p) => p.source === "custom").length}</span></TabsTrigger></TabsList></Tabs><div className="projects-grid">{filtered.map((p) => <Card key={p.id} className="project-card"><CardHeader><div className="project-card-top"><Badge variant="outline" className="source-badge">{p.source}</Badge><CardDescription>{new Intl.DateTimeFormat("ko-KR").format(new Date(p.updatedAt))}</CardDescription></div><CardTitle>{p.title}</CardTitle></CardHeader><CardContent><p className="project-description">{p.description}</p><div className="project-tags">{p.tags.map((tag) => <Badge key={tag} variant="secondary">{tag}</Badge>)}</div></CardContent><CardFooter className="project-footer"><span className="project-stats"><span><Star size={14} /> {p.stars}</span><span><GitFork size={14} /> {p.forks}</span></span><span className="project-actions"><a className="project-icon-button" href={p.repoUrl} target="_blank" rel="noopener noreferrer" aria-label={`${p.title} repository`}><GitBranch /></a>{p.demoUrl && <a className="project-icon-button" href={p.demoUrl} target={p.demoUrl.startsWith("/") ? "_self" : "_blank"} rel="noopener noreferrer" aria-label={`${p.title} demo`}><ExternalLink /></a>}</span></CardFooter></Card>)}</div>{filtered.length === 0 && <p className="projects-empty">이 카테고리에는 프로젝트가 없습니다.</p>}</div>; }
