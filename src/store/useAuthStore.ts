import { create } from "zustand";
type AuthState = { isAdmin: boolean; isLoginModalOpen: boolean; isLoading: boolean; setAdmin: (status: boolean) => void; openLoginModal: () => void; closeLoginModal: () => void; checkAuthStatus: () => Promise<void>; logout: () => Promise<void> };
export const useAuthStore = create<AuthState>((set) => ({ isAdmin: false, isLoginModalOpen: false, isLoading: false, setAdmin: (status) => set({ isAdmin: status }), openLoginModal: () => set({ isLoginModalOpen: true }), closeLoginModal: () => set({ isLoginModalOpen: false }), checkAuthStatus: async () => { set({ isLoading: true }); try { const response = await fetch("/api/auth/status"); const data = await response.json(); set({ isAdmin: Boolean(data.authenticated) }); } finally { set({ isLoading: false }); } }, logout: async () => { await fetch("/api/auth/logout", { method: "POST" }); set({ isAdmin: false, isLoginModalOpen: false }); } }));
